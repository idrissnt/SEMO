/// Configuration settings for the application
class AppConfig {
  /// Whether the app should use mock data instead of making real API calls
  /// Set this to true to run the app without any network requests
  static bool useOfflineMode = true;

  /// Whether to show debug information in the app
  static bool showDebugInfo = false;
}
